<%= form_with(model: my_course) do |form| %>
  <% if my_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_course.errors.count, "error") %> prohibited this my_course from being saved:</h2>

      <ul>
        <% my_course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
